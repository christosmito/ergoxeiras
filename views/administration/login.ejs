<%- include("../includes/head.ejs") %>
<link rel="stylesheet" href="../../css/style.css">
<link rel="stylesheet" href="../../css/create-user-style.css">
</head>

<body>
    <div id="nav-container">
        <nav class="navbar">
              <div id="flex-container">
                <a id="logo" class="navbar-brand" href="/">ErgoXeiras</a>
              
                <div class="bar-container" onclick="myFunction(this)">
                  <div class="bar1"></div>
                  <div class="bar2"></div>
                  <div class="bar3"></div>
                </div>
              </div>
          
              <div class="menu">
                  <ul>
                      <li>
                        <a href="/">Αρχική</a>
                      </li>
                      <li>
                        <a href="/#categories">Έργα</a>
                      </li>
                      <!-- <li class="nav-item">
                      <a class="nav-link" href="#">Σχετικα</a>
                      </li> -->
                      <li>
                        <a href="/#communication-main-container">Επικοινωνία</a>
                      </li>
                      <li>
                        <a href="https://www.facebook.com/ergoxeiras" target="_blank"><i class="fab fa-facebook-square fa-2x"></i></a><!--target="_blank" opens a new window-->
                      </li>
                      <% if(isLoggedIn) { %>
                            <li class="nav-item">
                                <a class="nav-link" href="admin/logout">Αποσύνδεση</a>
                            </li>
                        <% } %>
                  </ul>
              </div>
        </nav>
      </div>
        <script>
      function myFunction(x) {
        x.classList.toggle("change");
        const menu = document.querySelector(".menu");
        console.log(menu);
        menu.classList.toggle("show");
      }
      </script>
      
    <h1>Παρακαλω συνδεθειτε</h1>

    <% if(error) { %>
        <p id="error"><%= error %></p>
    <% } %>

    <div id="container">
        <form method="POST" action="" novalidate>
            <div class="form-group">
            <input type="email" class="form-control" name="email" placeholder="e-mail">
            </div>
            <div class="form-group">
            <input type="password" class="form-control" name="password" placeholder="Κωδικός πρόσβασης">
            </div>
            <!-- <div class="form-group">
            <label for="exampleInputEmail1">Επιβεβαιωση κωδικου</label>
            <input type="password" class="form-control" id="password"">
            </div> -->
            <div>
                <button type="submit" class="btn btn-dark btn-lg text-center">Συνδεση</button>
            </div>
            <input type="hidden" name="_csrf" value="<%= csrf %>">
            <p>Δημιουργείστε λογαριασμό <a href="create-user">εδώ</a></p>
        </form>
    </div>

</body>
